<%= form_for ([@post, @post.comments.build]) do |f| %>
  <p>
    <%= f.label :message %><br>
    <%= f.text_area :message , class: "form-control"%>
  </p>
  <p>
 
  <p>
    <%= f.submit %>
  </p>
<% end %>